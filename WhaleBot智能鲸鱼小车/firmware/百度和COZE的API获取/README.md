# 🔑 API Key 获取简易指南

---

本项目需要 **百度智能云**（负责听和说）和 **Coze 扣子**（负责思考）的 API 密钥才能运行。请按照以下步骤获取，并将它们填入 `WhaleBot.ai_Main.ino` 代码中。

> ℹ️ **提示**：本文仅为快速 Key 获取指引，详细的开发与代码解析请参考仓库内的完整技术文档。

---

## 1. 百度智能云 (语音识别 & 合成)

用于实现小车的“耳朵”（STT）和“嘴巴”（TTS）。

1.  **注册登录**：访问 [百度智能云控制台](https://console.bce.baidu.com/ai/)。
2.  **创建应用**：
    *   进入“语音技术”控制台。
    *   点击“创建应用”。
    *   **接口选择**：务必勾选 **“短语音识别”** 和 **“在线语音合成”**。
3.  **获取密钥**：
    *   创建成功后，在应用列表中找到你的应用。
    *   复制 **`API Key`** 和 **`Secret Key`**。

---

## 2. Coze 扣子 (AI 大模型)

用于实现小车的“大脑”，进行自然语言对话。

1.  **注册登录**：访问 [Coze 国内版](https://www.coze.cn/)。
2.  **创建 Bot**：
    *   创建一个新的智能体 (Bot)，编写提示词（例如：“你是一个叫乐迪的智能小车...”）。
    *   **重要**：配置完成后，必须点击右上角的 **“发布”**，否则 API 无法调用。
3.  **获取 Bot ID**：
    *   进入你的 Bot 开发页面。
    *   看浏览器地址栏：`https://www.coze.cn/space/xxxx/bot/73123456789...`
    *   链接末尾的那串数字就是 **`Bot ID`**。
4.  **获取 PAT (访问令牌)**：
    *   点击左下角头像 -> `扣子 API` -> `API 令牌`。
    *   点击“添加令牌” (Personal Access Token)。
    *   勾选权限（建议全选或至少包含 Bot 对话权限），复制生成的 **`PAT`** 字符串。

---

## 📝 填入代码

拿到上述 4 个密钥后，请打开 Arduino IDE 中的 `WhaleBot.ai_Main.ino`，找到顶部的 **用户配置区** 进行替换：

```cpp
// 百度配置
const char* baidu_api_key    = "复制你的API Key到这里";       
const char* baidu_secret_key = "复制你的Secret Key到这里"; 

// Coze 配置
const String coze_apiKey = "复制你的PAT到这里"; 
const String botId       = "复制你的Bot ID到这里";
